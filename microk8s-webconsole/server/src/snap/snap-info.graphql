type ServiceInfo {
    name: String!
    mode: String!
    status: String!
}

type Query {
    getServiceInfo: [ServiceInfo]
}

type Mutation {
    setServiceStatus(name: String!, enabled: Boolean!): [ServiceInfo]
}